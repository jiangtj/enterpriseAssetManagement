spring.application.name=asset
spring.profiles.active=dev
# spring.jackson.serialization.write_dates_as_timestamps=false
# server.context-path=/app
server.port=8080
server.session.timeout=36000

mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.jtj.web.entity
mybatis.config-location=classpath:config/mybatis-config.xml


# 参考了些大公司的架构，如activiti6，分为activiti-app,activiti-rest,activiti-admin
# 大多都采用多模块的设计，当然这是十分有用的
# 同时我也意识到该项目的设计存在一定的缺陷
# 为此，我在此版本中移除了spring cloud
# 接下来的工作为完善各个功能
# 而架构方面的调整将暂停
# 如果需要使用spring cloud的请参照此次提交，恢复注释部分
#
# 预告：
# 下版本可能会重新创建项目空间开发，届时可能分为eureka，rest，app三部分


# eureka
#eureka.client.serviceUrl.defaultZone = http://localhost:8761/eureka/
#eureka.client.enabled=false
#feign.hystrix.enabled=true

# 网点默认配置
#point.client.enabled=false
#point.client.name=point-client
#point.client.url=
#point.client.path=/point/get
#point.client.username=
#point.client.password=

